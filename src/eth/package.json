{
  "name": "poly-wrapper",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "compile": "truffle compile",
    "migrate": "rm -rf ./build & truffle migrate --reset --network bsc",
    "test": "truffle test"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^3.3.0",
    "@openzeppelin/test-helpers": "^0.5.6",
    "@truffle/hdwallet-provider": "^1.2.0"
  }
}
